{
  "schema_version": 1,
  "sections": [
    {"id": "mode", "title": "Enforcement", "fields": ["mode"]},
    {"id": "rules", "title": "Supported rules", "fields": ["custom_rules", "exclusions"]}
  ],
  "notices": ["Uses deterministic supported rules; does not claim full CRS or PCRE compatibility."]
}

